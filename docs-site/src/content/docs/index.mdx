---
title: FixYourDocs
description: Documentation for the FixYourDocs product — the open protocol, the SDKs, the CLI, the Hub, and the MCP server.
template: splash
hero:
  tagline: Documentation for the FixYourDocs product — the open protocol, the SDKs, the CLI, the Hub, and the MCP server.
  actions:
    - text: Get started
      link: /v0/getting-started/
      icon: right-arrow
      variant: primary
    - text: MCP server
      link: /v0/mcp/
      icon: puzzle
      variant: secondary
    - text: The FixYourDocs Hub
      link: /v0/hub/
      icon: rocket
      variant: secondary
    - text: The protocol
      link: /v0/protocol/
      icon: open-book
      variant: secondary
---

import { Card, CardGrid } from '@astrojs/starlight/components';

When an AI agent reads documentation and the docs are wrong, stale,
or incomplete, the failure is usually silent — the agent guesses, retries,
or gives up, and the maintainer never hears about it. **FixYourDocs** turns
that silent failure into a structured signal: an agent (or a human) files a
report against the exact doc page, and it lands as a GitHub Issue on the
maintainer's repo.

<video
  controls
  playsinline
  preload="metadata"
  poster="/demo-poster.png"
  width="540"
  height="540"
  style="width:100%;max-width:540px;height:auto;display:block;margin:1.5rem auto;border-radius:12px;border:1px solid var(--sl-color-gray-5);background:#0f172a;"
  aria-label="Demo: an AI agent hits a stale doc, fixes its own code, and files a structured report that opens a GitHub issue."
>
  <source src="/demo.mp4" type="video/mp4" />
  <a href="/demo.mp4">Watch the demo</a>.
</video>

It works from **both sides**. If you **maintain docs**, you wire your repo so
agents working in it report *your* docs and you receive the Issues
(**Mode A**). If you **build with agents**, you wire your own agent once,
globally, so it offers — with your OK, on public docs only — to report the
**third-party** docs it consults across every project (**Mode B**). Either
way, the same report flows to whoever owns the doc.

This site documents every piece of the product. New here? The
[Getting started](/v0/getting-started/) guide takes you from zero to a
visible GitHub issue in under five minutes.

<CardGrid>
  <Card title="Python SDK" icon="seti:python">
    `pip install fixyourdocs` — build and send reports from Python.
    [Python SDK →](/v0/sdk/python/)
  </Card>
  <Card title="TypeScript SDK" icon="seti:typescript">
    `npm install @fixyourdocs/sdk` — the same API for Node.
    [TypeScript SDK →](/v0/sdk/typescript/)
  </Card>
  <Card title="CLI" icon="seti:powershell">
    `npx @fixyourdocs/sdk init` and `npx @fixyourdocs/sdk report` — no code required.
    [CLI →](/v0/cli/)
  </Card>
  <Card title="MCP server" icon="puzzle">
    One tool, `file_doc_feedback`, for Claude Desktop, Cursor, and Codex.
    [MCP server →](/v0/mcp/)
  </Card>
  <Card title="AGENTS.md snippet" icon="document">
    A paste-ready block that wires any agent into the protocol.
    [AGENTS.md snippet →](/v0/agents-md/)
  </Card>
  <Card title="The Hub" icon="rocket">
    `hub.fixyourdocs.io` receives reports and forwards them to GitHub.
    [The Hub →](/v0/hub/)
  </Card>
</CardGrid>

## How the pieces fit together

1. An agent reads some docs — yours, or a third-party library's — and hits a
   problem.
2. It files a v0 [Docs Feedback Protocol](/v0/protocol/) report — via the
   [SDK](/v0/sdk/python/), the [CLI](/v0/cli/), the [MCP server](/v0/mcp/),
   or the plain `curl` in the [AGENTS.md snippet](/v0/agents-md/).
3. The report is `POST`ed to the [Hub](/v0/hub/) at
   `hub.fixyourdocs.io/v1/reports`.
4. If you've connected the Hub to your repo and verified you own the doc's
   domain, the report appears as a **GitHub Issue** you can triage.

The protocol is open and the SDKs are Apache-2.0 — the Hub is one way to
receive reports, not the only way.
